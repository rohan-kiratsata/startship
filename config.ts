/* 
    TEMPLATE CONFIGURATION - manage and configure the template as you want.
 */

import { StylingConfig, TemplateConfig } from "@/types";

export const TEMPLATE_CONFIG: TemplateConfig = {
  name: "Startup Boilerplate",
  description:
    "The ultimate boilerplate for your startup to launch your product within hours",
};

export const STYLING_CONFIG: StylingConfig = {
  theme: "light", // Set your favorite theme here. DEFAULT : "dark"
};
